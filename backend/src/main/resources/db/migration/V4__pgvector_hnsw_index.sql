-- =============================================================================
-- Flyway Migration V4: Pgvector HNSW Vector Index for Fast Cosine Similarity
-- =============================================================================

-- HNSW Vector Index Initialization (PostgreSQL: HNSW index / H2: standard index placeholder)
${hnsw-index-init}
